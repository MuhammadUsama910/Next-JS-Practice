export default function Login() {

  return(
    <>
      <h1> Login </h1>
    </>
  )
}

//file inside a route group (auth). The bracket's indicate that auth folder is a route group
// that's why there is no need to write auth in URL. you can directly write
// localhost:3000/login or localhost:3000/register or localhost:3000/forgot-password
// in the URL to get access to routes.  